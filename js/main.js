let phone={
    name:"",
    network:"",
    price:"",
}
console.log(phone);
phone.name=prompt(("enter the name"));
phone.network=prompt(("enter the network"));
phone.price=prompt(("enter the price"));
console.log(phone);